import { useNavigate } from "react-router-dom";

export default function AdminDashboard() {
    const navigate = useNavigate()
    navigate('/teachersClass');
    return (
        <div className="w-full h-full ">
            
        </div>
    )
}